using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[SelectionBase()]
public class SelectionBase : MonoBehaviour
{
   //Empty class, makes sure the item in editor selected is the root parent of an object and not a child object
}
